[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quantara"
version = "0.1.0"
description = "Ketchums quantum research labs"
dependencies = ["numpy"]
